<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MediiProiectGym.AboutPage"
             Title="AboutPage">
    <VerticalStackLayout Spacing="10" Margin="10">
        <HorizontalStackLayout Spacing="10">
            <Image Source="shop_list.png"
 SemanticProperties.Description="Workout Helper"
HeightRequest="64" />
            <Label FontSize="22" FontAttributes="Bold" Text="My Workout Routines" VerticalOptions="End" />
            <Label FontSize="22" Text="v2.6" VerticalOptions="End" />
        </HorizontalStackLayout>
        <Label Text="Most renowned app for increasing your gains when training at the gym!" />

    </VerticalStackLayout>
</ContentPage>